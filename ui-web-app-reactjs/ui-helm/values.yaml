
replicaCount: 1

image:
  repository: tbalaji/dockerimages
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

service:
  type: LoadBalancer
  port: 80
  targetport: 8080

container:
  name: Node-ui
